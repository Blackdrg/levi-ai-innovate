name: Deploy Frontend to Firebase Hosting

on:
  push:
    branches:
      - main

    paths:
      - 'frontend/**'
      - 'firebase.json'
      - '.github/workflows/deploy-frontend.yml'

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: npm install
        working-directory: frontend

      - name: Inject Secrets into Configuration
        run: |
          sed -i "s/__FIREBASE_API_KEY__/${{ secrets.FIREBASE_API_KEY }}/g" js/config.js
          sed -i "s/__FIREBASE_AUTH_DOMAIN__/${{ secrets.FIREBASE_AUTH_DOMAIN }}/g" js/config.js
          sed -i "s/__FIREBASE_PROJECT_ID__/${{ secrets.FIREBASE_PROJECT_ID }}/g" js/config.js
          sed -i "s/__FIREBASE_STORAGE_BUCKET__/${{ secrets.FIREBASE_STORAGE_BUCKET }}/g" js/config.js
          sed -i "s/__FIREBASE_MESSAGING_SENDER_ID__/${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}/g" js/config.js
          sed -i "s/__FIREBASE_APP_ID__/${{ secrets.FIREBASE_APP_ID }}/g" js/config.js
        working-directory: frontend

      - name: Build Assets
        run: npm run build
        working-directory: frontend
        env:
          NODE_ENV: production

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_LEVI_AI_C23C6 }}'
          channelId: live
          projectId: levi-ai-c23c6

      - name: Verify Frontend Status
        run: |
          echo "Verifying Hosting at https://levi-ai-c23c6.web.app ..."
          # Max retries: 5, Wait: 5s
          for i in {1..5}; do
            echo "Attempt $i..."
            curl -f https://levi-ai-c23c6.web.app && exit 0
            echo "Frontend check failed, retrying in 5 seconds..."
            sleep 5
          done
          echo "Frontend verification FAILED"
          exit 1
