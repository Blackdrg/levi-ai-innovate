apiVersion: apps/v1
kind: Deployment
metadata:
  name: levi-brain-v12
  labels:
    app: levi-brain
spec:
  replicas: 3
  minReadySeconds: 15
  revisionHistoryLimit: 5
  progressDeadlineSeconds: 600
  selector:
    matchLabels:
      app: levi-brain
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: levi-brain
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: brain
        image: gcr.io/levi-ai/brain:latest
        ports:
        - containerPort: 8000
        env:
        - name: ENVIRONMENT
          value: production
        envFrom:
        - configMapRef:
            name: brain-config
        - secretRef:
            name: brain-secrets
        resources:
          requests:
            cpu: "500m"
            memory: "1Gi"
          limits:
            cpu: "1000m"
            memory: "2Gi"
        startupProbe:
          httpGet:
            path: /ready
            port: 8000
          failureThreshold: 30
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 5
        lifecycle:
          preStop:
            exec:
              command: ["/bin/sh", "-c", "sleep 10"]
---
apiVersion: v1
kind: Service
metadata:
  name: levi-brain-api
spec:
  selector:
    app: levi-brain
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: LoadBalancer
