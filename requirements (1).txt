# ── Web framework ───────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
gunicorn>=22.0.0
python-multipart>=0.0.9
slowapi>=0.1.9

# ── Database ─────────────────────────────────
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9
pgvector>=0.3.0

# ── Cache / Queue ────────────────────────────
redis>=5.0.4
celery>=5.3.0

# ── Auth ─────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
itsdangerous>=2.1.2

# ── Payments ─────────────────────────────────
stripe>=7.0.0

# ── Cloud Storage ────────────────────────────
boto3>=1.34.0

# ── AI / ML ──────────────────────────────────
transformers>=4.40.0
sentence-transformers>=3.0.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
numpy>=1.26.4
scikit-learn>=1.4.0

# ── Image generation ─────────────────────────
pillow>=10.3.0
requests>=2.31.0

# ── Video generation (optional) ──────────────
moviepy>=1.0.3

# ── Translation ──────────────────────────────
mtranslate>=1.8

# ── Config ───────────────────────────────────
python-dotenv>=1.0.1
pydantic>=2.7.0

# ── Email ────────────────────────────────────
# resend is called via HTTP, no package needed

# ── Testing ──────────────────────────────────
pytest>=8.2.0
httpx>=0.27.0
pytest-asyncio>=0.23.6

# ── Production server ────────────────────────
waitress>=3.0.0
