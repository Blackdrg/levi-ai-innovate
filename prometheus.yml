# prometheus.yml
global:
  scrape_interval: 15s
rule_files:
  - infrastructure/prometheus/alerts.yml

scrape_configs:
  - job_name: 'levi_api'
    static_configs:
      - targets: ['backend:8000']

  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
