{
  "name": "LEVI-AI",
  "version": "14.0.0",
  "description": "Autonomous-SOVEREIGN production-grade Sovereign OS",
  "author": "Sovereign Engineering",
  "license": "Sovereign-1.0",
  "scripts": {
    "server": "python -m uvicorn backend.api.main:app --host 127.0.0.1 --port 8000",
    "client": "python -m http.server 8080 --bind 127.0.0.1 --directory frontend",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build:frontend": "npm run --prefix frontend build",
    "audit:sovereign": "python tests/graduation_audit_extended.py"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "pako": "^2.1.0",
    "groq-sdk": "^0.5.0",
    "together": "^0.6.0"
  }
}
